# App Icon

Put your app icon here as `app_icon.png`.

**Recommended specifications:**
- Format: PNG
- Size: 1024 x 1024 pixels
- Transparency: Supported (for Android adaptive icon)
- Color space: sRGB

After placing the icon, run:
```bash
flutter pub get
dart run flutter_launcher_icons
```
