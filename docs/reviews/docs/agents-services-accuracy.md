# AGENTS.md Services Accuracy Review

Scope: verified `AGENTS.md` as documentation, not as ground truth, against `lib/services/` plus directly related providers, UI entry points, and Windows/Android platform files.

Classification legend: accurate, outdated, missing, contradicted, unclear.

## Confirmed Accurate Claims

| Class | Finding | AGENTS.md evidence | Code evidence |
|---|---|---|---|
| accurate | Download task deduplication is path-based, and completed downloads only save the path after the file exists. | Download System says "Path deduplication by `savePath`" and "File verification before saving path" at `AGENTS.md:366-368`. | `DownloadRepository.getTasksBySavePaths()` indexes tasks by `savePath` at `lib/data/repositories/download_repository.dart:45-59`; `DownloadService.addTracksDownload()` filters existing and duplicate paths at `lib/services/download/download_service.dart:448-483`; completion checks `File(savePath).exists()` before `completeTaskWithDownloadPath()` at `lib/services/download/download_service.dart:901-913`. |
| accurate | Download folder layout and per-source header construction match the document. | Download System lists `audio.m4a`/`P{NN}.m4a`, metadata, cover/avatar, and `buildDownloadMediaHeaders()` / `buildDownloadImageHeaders()` at `AGENTS.md:370-375`. | Audio paths are computed as `audio.m4a` or `P{NN}.m4a` at `lib/services/download/download_path_utils.dart:37-47`; metadata, cover, and avatar files are written at `lib/services/download/download_service.dart:1224-1259`; download media/image headers are built through `SourceHttpPolicy.mediaHeaders()` at `lib/services/download/download_media_headers.dart:4-21`. |
| accurate | Android storage permission behavior is documented correctly. | Download System says Android custom directories use `MANAGE_EXTERNAL_STORAGE`, default base dir is `Music/FMP`, and checks use app-owned MethodChannel instead of `permission_handler` at `AGENTS.md:376`. | Android base dir uses `Music/FMP` at `lib/services/download/download_path_utils.dart:122-149`; directory selection calls `StoragePermissionService` on Android at `lib/services/download/download_path_manager.dart:26-37`; the service documents the MethodChannel/non-`permission_handler` rationale at `lib/services/storage_permission_service.dart:22-31`; Android declares `MANAGE_EXTERNAL_STORAGE` at `android/app/src/main/AndroidManifest.xml:22`. |
| accurate | Lyrics auto-match order, enabled-source filtering, plain-lyrics gate, and AI fallback behavior are reflected in code. | Lyrics System defines existing match, Netease direct fetch, original platform direct fetch, source priority/disabled sources, AI modes, no regex fallback after valid AI parse failure, and `allowPlainLyricsAutoMatch=false` at `AGENTS.md:272-284`. | `tryAutoMatch()` checks existing match, enabled sources, Netease direct fetch, original ID direct fetch, AI modes, and regex fallback branches at `lib/services/lyrics/lyrics_auto_match_service.dart:88-189`; synced/plain filtering is at `lib/services/lyrics/lyrics_auto_match_service.dart:218-222`; AI advanced matching returns false for no candidates/no selection/unknown candidate and only falls back when selector is unavailable at `lib/services/lyrics/lyrics_auto_match_service.dart:376-423`. |
| accurate | Lyrics title parse cache is saved and cleared on startup. | Lyrics System says successful title parses are stored in `LyricsTitleParseCache` and the cache is cleared on startup at `AGENTS.md:284`. | AI parses are saved through `titleParseCacheRepo.save()` at `lib/services/lyrics/lyrics_auto_match_service.dart:564-587`; startup clears `isar.lyricsTitleParseCaches` at `lib/providers/database_provider.dart:186`. |
| accurate | External search-match playlist imports support Netease, QQ Music, and Spotify as described. | External Playlist Import lists Netease standard/short links, QQ Music URL formats with `QQMusicSign`, and Spotify `__NEXT_DATA__` embed parsing at `AGENTS.md:223-226`. | Netease handles `music.163.com`, `163cn.tv`, and resolves short links at `lib/data/sources/playlist_import/netease_playlist_source.dart:18-34` and `lib/data/sources/playlist_import/netease_playlist_source.dart:68-98`; QQ Music extracts multiple playlist URL formats and signs requests with `QQMusicSign.encrypt()` at `lib/data/sources/playlist_import/qq_music_playlist_source.dart:23-43` and `lib/data/sources/playlist_import/qq_music_playlist_source.dart:120-126`; Spotify reads the embed page and parses `__NEXT_DATA__` at `lib/data/sources/playlist_import/spotify_playlist_source.dart:44-67` and `lib/data/sources/playlist_import/spotify_playlist_source.dart:90-112`. |
| accurate | Ranking cache UI uses immutable `RankingCacheState` and notifier methods. | State Management rules say ranking cache UI must watch `RankingCacheState` and call refresh/timer methods through `rankingCacheServiceProvider.notifier` at `AGENTS.md:124`. | `RankingCacheState.copyWith()` wraps lists in `List.unmodifiable()` at `lib/services/cache/ranking_cache_service.dart:39-60`; the provider is `StateNotifierProvider<RankingCacheService, RankingCacheState>` at `lib/services/cache/ranking_cache_service.dart:225-239`; home/explore read state through providers/selectors and refresh through `.notifier` at `lib/providers/popular_provider.dart:134-251` and `lib/ui/pages/explore/explore_page.dart:122-140`. |
| accurate | Radio retained context and active playback ownership are implemented. | Radio Return and Ownership defines `hasCurrentStation`, `hasActivePlaybackOwnership`, `isRadioPlayingProvider`, Home tap behavior, and pausing music before radio loading at `AGENTS.md:338-344`. | `RadioState.hasCurrentStation` and `hasActivePlaybackOwnership` are implemented at `lib/services/radio/radio_controller.dart:128-134`; `isRadioPlayingProvider` exposes active ownership at `lib/services/radio/radio_controller.dart:1134-1137`; `RadioController.play()` pauses music before setting radio loading state at `lib/services/radio/radio_controller.dart:429-455`; Home uses retained radio context for Now Playing tap actions at `lib/ui/pages/home/home_page.dart:1444-1477`. |
| accurate | Windows lyrics sub-window plugin registration excludes tray and hotkey plugins. | Sub-Window Plugin Registration says `RegisterPluginsForSubWindow()` excludes `tray_manager` and `hotkey_manager` due global static channels at `AGENTS.md:383-384`. | Main windows register all generated plugins, including tray and hotkey, at `windows/flutter/generated_plugin_registrant.cc:21-43`; sub-windows use selective registration and explicitly exclude TrayManager/HotkeyManager at `windows/runner/flutter_window.cpp:15-40`; the desktop_multi_window callback calls that registrar at `windows/runner/flutter_window.cpp:64-72`. |

## Outdated Or Conflicting Claims

| Class | Finding | AGENTS.md evidence | Code evidence |
|---|---|---|---|
| contradicted | The ranking data-loading pattern line is stale: it says Home/explore rankings use `CacheService` + `StreamProvider`, but current code uses `RankingCacheService` as a `StateNotifierProvider`. | Data Loading Patterns lists "API + cache" as "`CacheService` + `StreamProvider`" for Home/explore rankings at `AGENTS.md:117`, while the later ranking rule references `RankingCacheState` at `AGENTS.md:124`. | No `CacheService` ranking provider exists in the searched code; current service extends `StateNotifier<RankingCacheState>` at `lib/services/cache/ranking_cache_service.dart:64-81`, and the provider is `StateNotifierProvider` at `lib/services/cache/ranking_cache_service.dart:225-239`. |
| contradicted | Bilibili thumbnail suffix documentation is stale. The document says square width/height suffix; code emits a width-only suffix to preserve aspect ratio. | Image Thumbnail Optimization says Bilibili uses `@{size}w_{size}h.jpg` at `AGENTS.md:386-389`. | `_optimizeBilibiliUrl()` strips existing suffixes and returns `$baseUrl@${size}w.jpg` at `lib/core/utils/thumbnail_url_utils.dart:97-110`. |
| contradicted | YouTube thumbnail documentation is broader than current implementation. The document mentions default/mq/hq/sd/maxres plus webp; code intentionally uses only 16:9 `mqdefault` or `maxresdefault`, and does not force webp unless the original URL is already webp. | Image Thumbnail Optimization says YouTube uses quality tiers and webp at `AGENTS.md:389`. | `_selectYouTubeQuality()` documents and returns only `mqdefault` or `maxresdefault` at `lib/core/utils/thumbnail_url_utils.dart:184-194`; `_optimizeYouTubeThumbnail()` preserves existing jpg/webp rather than forcing webp at `lib/core/utils/thumbnail_url_utils.dart:152-181`. |
| outdated | The Account System row for Bilibili lists only WebView cookie extraction, but Bilibili login currently supports both WebView and QR code. | Account System table lists Bilibili login as "WebView cookie extraction" at `AGENTS.md:288-294`. | `BilibiliLoginPage` is explicitly "WebView + QR" and has two tabs at `lib/ui/pages/settings/bilibili_login_page.dart:14-63`; `BilibiliAccountService` implements WebView cookie login at `lib/services/account/bilibili_account_service.dart:83-119` and QR generation/polling at `lib/services/account/bilibili_account_service.dart:122-184`. |

## Missing Important Behaviors

| Class | Finding | AGENTS.md evidence | Code evidence |
|---|---|---|---|
| missing | The Download System section does not mention resumable `.downloading` temp files, resume offsets, or explicit path sync/maintenance services. | Download System covers dedupe, verification, isolate progress, layout, headers, and Android permission at `AGENTS.md:366-376`; File Structure only lists `download_service.dart`, `download_path_manager.dart`, and `download_path_utils.dart` at `AGENTS.md:480-483`. | Resume uses `$savePath.downloading`, `task.canResume`, and temp-file length at `lib/services/download/download_service.dart:727-757`; `DownloadPathSyncService` scans local files and replaces DB paths at `lib/services/download/download_path_sync_service.dart:11-30`; `DownloadPathMaintenanceService` handles path maintenance/deletion results at `lib/services/download/download_path_maintenance_service.dart:11-44`; providers expose both services at `lib/providers/download_path_provider.dart:21-39`. |
| missing | Account credentials storage is important but absent from the Account System table. Isar `Account` stores non-sensitive status only; cookies/tokens live in secure storage. | Account System table lists login method and token names at `AGENTS.md:288-294` but does not state storage boundaries. | `Account` documents that Cookie/Token credentials are stored in `flutter_secure_storage` at `lib/data/models/account.dart:7-10`; account services write credentials through `FlutterSecureStorage` at `lib/services/account/bilibili_account_service.dart:105-110`, `lib/services/account/youtube_account_service.dart:77-83`, and `lib/services/account/netease_account_service.dart:436-444`. |
| missing | File Structure Highlights omit active account and lyrics service helpers that agents are likely to need. | File Structure lists only four account files at `AGENTS.md:462-466` and selected lyrics files at `AGENTS.md:467-477`. | Account providers import and expose `account_service.dart`, Bilibili favorites, Netease playlist, and YouTube playlist services at `lib/providers/account_provider.dart:11-17` and `lib/providers/account_provider.dart:21-58`; lyrics providers use `AiLyricsSelector` at `lib/providers/lyrics_provider.dart:8-46`; AI parser/selector share `openai_chat_endpoint.dart` at `lib/services/lyrics/ai_title_parser.dart:7` and `lib/services/lyrics/ai_lyrics_selector.dart:7`; popup style is modeled in `LyricsWindowStyle` at `lib/services/lyrics/lyrics_window_style.dart:66-108`. |
| missing | File Structure Highlights omit `StoragePermissionService`, even though the Download System relies on it. | Download System references `StoragePermissionService` at `AGENTS.md:376`, but the File Structure service tree does not list `lib/services/storage_permission_service.dart` at `AGENTS.md:449-492`. | `DownloadPathManager` imports and calls `StoragePermissionService` at `lib/services/download/download_path_manager.dart:4-6` and `lib/services/download/download_path_manager.dart:30-37`; the service owns the MethodChannel at `lib/services/storage_permission_service.dart:22-31`. |
| missing | Ranking cache behavior is more than "hourly refresh": it also loads immediately, times out initial load, refreshes on network recovery, and supports a user-configurable interval. | File Structure calls `ranking_cache_service.dart` "Home ranking cache (hourly refresh)" at `AGENTS.md:478-479`. | `RankingCacheService` documents startup fetch, hourly background refresh, network recovery refresh, and full-cache use at `lib/services/cache/ranking_cache_service.dart:12-19`; initialization has a 5s initial-load timeout at `lib/services/cache/ranking_cache_service.dart:83-104`; network recovery triggers `_refreshAll()` at `lib/services/cache/ranking_cache_service.dart:123-133`; settings update the interval through the notifier at `lib/providers/refresh_settings_provider.dart:55-75`. |
| missing | External playlist import supports additional short/link forms not called out: Netease `y.music.163.com`, QQ short links, and Spotify short links. | External Playlist Import lists Netease standard/`163cn.tv`, QQ multiple formats, and Spotify embed parsing at `AGENTS.md:223-226`. | Netease `canHandle()` also accepts `y.music.163.com` at `lib/data/sources/playlist_import/netease_playlist_source.dart:18-21`; QQ resolves `c.y.qq.com` and `url.cn` short links at `lib/data/sources/playlist_import/qq_music_playlist_source.dart:96-112`; Spotify resolves `spotify.link` at `lib/data/sources/playlist_import/spotify_playlist_source.dart:19-23` and `lib/data/sources/playlist_import/spotify_playlist_source.dart:70-75`. |
| missing | Windows sub-window docs omit the `window_manager` caveat: it is included despite similar global-channel issues, and the lyrics window bypasses close events by sending `requestHide`. | Sub-Window Plugin Registration only mentions excluding `tray_manager` and `hotkey_manager` at `AGENTS.md:383-384`. | `windows/runner/flutter_window.cpp` documents that `window_manager` also has C++ to Dart event-chain issues but is needed by sub-windows at `windows/runner/flutter_window.cpp:19-21`; it is still registered at `windows/runner/flutter_window.cpp:35-36`; the lyrics window close button sends `requestHide` instead of relying on window-manager close events at `lib/ui/windows/lyrics_window.dart:394-400` and `lib/ui/windows/lyrics_window.dart:860-865`, handled by `LyricsWindowService` at `lib/services/lyrics/lyrics_window_service.dart:404-407`. |

## Expected Architecture Rules Not Currently Followed

| Class | Finding | AGENTS.md evidence | Code evidence |
|---|---|---|---|
| contradicted | The only clear architecture-rule mismatch in this scope is the stale ranking data-loading rule: Home/explore rankings no longer follow the documented `CacheService` + `StreamProvider` pattern. Because `AGENTS.md` also separately requires immutable `RankingCacheState`, this looks like an internal docs conflict rather than a code defect. | Data Loading Patterns says rankings use `CacheService` + `StreamProvider` at `AGENTS.md:117`; ranking rule says use `RankingCacheState` from `rankingCacheServiceProvider` at `AGENTS.md:124`. | Current implementation is `RankingCacheService extends StateNotifier<RankingCacheState>` at `lib/services/cache/ranking_cache_service.dart:64-81`, with UI selectors in `lib/providers/popular_provider.dart:134-251`. |

## Unclear Items Needing Human Decision

| Class | Finding | AGENTS.md evidence | Code evidence |
|---|---|---|---|
| unclear | The download isolate wording may be motivation rather than behavior. The document says "Windows downloads run in an isolate"; code uses the isolate path unconditionally for downloads. | Download System says "Windows downloads run in an isolate" at `AGENTS.md:369`. | `_startDownload()` has no platform guard around `Isolate.spawn()` and describes isolate use for network I/O at `lib/services/download/download_service.dart:759-773`. |
| unclear | Decide whether File Structure Highlights should stay intentionally partial or become an agent navigation map. Several active services in the reviewed scope are omitted. | The section is titled "File Structure Highlights" at `AGENTS.md:449`, then lists selected services at `AGENTS.md:462-486`. | Actual `lib/services/` includes additional reviewed-scope files such as `lib/services/download/download_media_headers.dart:4-21`, `lib/services/import/youtube_mix_shorthand.dart:20-23`, `lib/services/account/account_service.dart:18-40`, and `lib/services/lyrics/ai_lyrics_selector.dart:72-73`. |
| unclear | Thumbnail behavior needs a decision: update docs to match the current aspect-preserving/16:9 implementation, or change code to the documented square Bilibili and broader YouTube tier behavior. Current code comments suggest the implementation is intentional. | Image Thumbnail Optimization describes Bilibili square suffix and YouTube tiers/webp at `AGENTS.md:386-390`. | Current Bilibili code returns width-only `@${size}w.jpg` at `lib/core/utils/thumbnail_url_utils.dart:97-110`; current YouTube code intentionally avoids 4:3 tiers at `lib/core/utils/thumbnail_url_utils.dart:184-194`. |
